---
draft: true
summary: >-
  Pod 是 Kubernetes 中最小的可调度单元，Service 是 Kubernetes 中提供服务发现和负载均衡功能的抽象概念，Kubectl 是
  Kubernetes 的命令行工具，用于管理和操作 Kubernetes 集群。


  可以使用 `kubectl create deployment` 命令发布应用到 Kubernetes，可以使用 `kubectl get
  deployments` 命令列出已发布的应用，可以使用 `kubectl get pods` 命令列出所有 pods。
---

## Pod

## Service

## Kubectl

### 命令

- `kubectl create deloyment [name] [image loction]` 发布应用
- `kubectl get deloyments` 列出已发布的应用
- `kubectl get pods` 列出所有 pods
- `kubectl describe pods`
- `kubectly apply -f [fileDir]` 创建服务
- `kubectl get namespace` 列出所有命名空间
- `kubectl create namespace <insert-namespace-name-here>`

### 发布应用到 Kubernetes
