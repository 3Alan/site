---
draft: true
summary: >-
  在 Kubernetes 中，Pod 是最小的可调度单元，Service 提供服务发现和负载均衡功能，Kubectl 是命令行工具，用于管理和操作
  Kubernetes 集群。


  可以使用 `kubectl create deployment` 命令发布应用，`kubectl get deployments`
  命令列出已发布的应用，`kubectl get pods` 命令列出所有 pods，`kubectl describe pods` 命令查看 pod
  的详细信息。
---

## Pod

## Service

## Kubectl

### 命令

- `kubectl create deloyment [name] [image loction]` 发布应用
- `kubectl get deloyments` 列出已发布的应用
- `kubectl get pods` 列出所有 pods
- `kubectl describe pods`
- `kubectly apply -f [fileDir]` 创建服务
- `kubectl get namespace` 列出所有命名空间
- `kubectl create namespace <insert-namespace-name-here>`

### 发布应用到 Kubernetes
