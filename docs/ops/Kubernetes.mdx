---
draft: true
summary: >-
  Pod 是 Kubernetes 中最小的可部署单元，由一个或多个容器组成。Service 是 Kubernetes 中的抽象概念，用于将 Pod
  暴露给外部世界。Kubectl 是 Kubernetes 的命令行工具，用于管理和操作 Kubernetes 集群。


  使用 kubectl 命令可以发布应用到 Kubernetes 集群，列出已发布的应用和 Pod，创建服务，列出所有命名空间。
---

## Pod

## Service

## Kubectl

### 命令

- `kubectl create deloyment [name] [image loction]` 发布应用
- `kubectl get deloyments` 列出已发布的应用
- `kubectl get pods` 列出所有 pods
- `kubectl describe pods`
- `kubectly apply -f [fileDir]` 创建服务
- `kubectl get namespace` 列出所有命名空间
- `kubectl create namespace <insert-namespace-name-here>`

### 发布应用到 Kubernetes
