---
summary: |-
  该 Markdown 文件主要介绍了如何使用 Python 的 `os` 模块来处理文件和目录。它提供了以下信息：

  * 如何使用 `os.path.join()` 函数来连接路径。
  * 如何使用 `os.path.exists()` 函数来检查文件或目录是否存在。
  * 如何使用 `os.makedirs()` 函数来创建目录。
  * 如何使用 `os.listdir()` 函数来获取目录中的文件和目录列表。
  * 如何使用 `os.remove()` 函数来删除文件。
---

