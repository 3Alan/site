---
draft: true
summary: |-
  Pod是Kubernetes中运行应用程序的最小单元，Service是用于将Pod暴露给外部的抽象层，kubectl是Kubernetes的命令行工具。

  kubectl命令包括：发布应用、列出已发布的应用、列出所有pods、描述pods、创建服务、列出所有命名空间、创建命名空间。

  发布应用到Kubernetes的步骤包括：创建一个deployment对象、创建一个service对象、等待deployment和service创建完成。
---

## Pod

## Service

## Kubectl

### 命令

- `kubectl create deloyment [name] [image loction]` 发布应用
- `kubectl get deloyments` 列出已发布的应用
- `kubectl get pods` 列出所有 pods
- `kubectl describe pods`
- `kubectly apply -f [fileDir]` 创建服务
- `kubectl get namespace` 列出所有命名空间
- `kubectl create namespace <insert-namespace-name-here>`

### 发布应用到 Kubernetes
